---
// Timer mockup showing active removal tracking
import PhoneFrame from './PhoneFrame.astro';
---

<PhoneFrame>
  <!-- Status bar -->
  <div class="bg-warning-500 px-6 py-3 flex items-center justify-between">
    <span class="text-white text-xs font-medium">9:41</span>
    <div class="flex items-center gap-1">
      <div class="w-4 h-2 bg-white/80 rounded-sm"></div>
      <div class="w-4 h-2 bg-white/60 rounded-sm"></div>
    </div>
  </div>
  
  <!-- Timer active state -->
  <div class="bg-gradient-to-b from-warning-500 to-warning-400 flex-1 flex flex-col items-center justify-center px-6 text-white">
    <!-- Status badge -->
    <div class="bg-white/20 backdrop-blur-sm px-4 py-2 rounded-full mb-6">
      <div class="flex items-center gap-2">
        <div class="w-2 h-2 bg-white rounded-full animate-pulse"></div>
        <span class="text-sm font-medium">Alineadores removidos</span>
      </div>
    </div>

    <!-- Big timer -->
    <div class="relative w-48 h-48 mb-8">
      <svg class="w-full h-full -rotate-90" viewBox="0 0 100 100">
        <circle cx="50" cy="50" r="45" fill="none" stroke="rgba(255,255,255,0.2)" stroke-width="6"/>
        <circle 
          cx="50" cy="50" r="45" 
          fill="none" 
          stroke="white" 
          stroke-width="6" 
          stroke-linecap="round" 
          stroke-dasharray="283" 
          stroke-dashoffset="212"
        />
      </svg>
      <div class="absolute inset-0 flex flex-col items-center justify-center">
        <span class="text-5xl font-bold tracking-tight">0:25</span>
        <span class="text-white/70 text-sm mt-1">minutos</span>
      </div>
    </div>

    <!-- Time remaining warning -->
    <div class="bg-white/10 backdrop-blur-sm rounded-2xl p-4 w-full max-w-xs mb-6">
      <div class="flex items-center justify-between">
        <div>
          <p class="text-white/70 text-xs">Tiempo restante hoy</p>
          <p class="text-xl font-bold">1h 35min</p>
        </div>
        <div class="w-12 h-12 bg-white/20 rounded-full flex items-center justify-center">
          <svg class="w-6 h-6" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z" />
          </svg>
        </div>
      </div>
    </div>

    <!-- Stop button -->
    <button class="bg-white text-warning-600 font-bold px-8 py-4 rounded-2xl shadow-lg flex items-center gap-3">
      <svg class="w-5 h-5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 12a9 9 0 11-18 0 9 9 0 0118 0z" />
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 10a1 1 0 011-1h4a1 1 0 011 1v4a1 1 0 01-1 1h-4a1 1 0 01-1-1v-4z" />
      </svg>
      Poner Alineadores
    </button>
  </div>

  <!-- Bottom tip -->
  <div class="bg-warning-600 px-6 py-4">
    <div class="flex items-start gap-3">
      <div class="w-8 h-8 bg-white/20 rounded-full flex items-center justify-center flex-shrink-0 mt-0.5">
        <svg class="w-4 h-4 text-white" fill="none" viewBox="0 0 24 24" stroke="currentColor">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z" />
        </svg>
      </div>
      <p class="text-white/90 text-xs leading-relaxed">
        Recordá: idealmente no deberías superar 2 horas sin tus alineadores para mantener el tratamiento en curso.
      </p>
    </div>
  </div>
</PhoneFrame>

